## Bandit Level 10 → Level 11

### Objective
Find the password for Bandit Level 11. The password is stored in `data.txt` and encoded using Base64.

### Commands Executed
- `ls`
- `base64 -d data.txt`

### Explanation
- Used `ls` to list files in the directory.
- Used `base64 -d` to decode the Base64-encoded contents of `data.txt`.
- The decoded output revealed the password.

### Password
  
`dtR173fZKb0RRsDFSGsg2RWnpNVj3qRr`



<img width="580" height="87" alt="image" src="https://github.com/user-attachments/assets/8bdd62bd-8fad-4b76-a2b0-6a7ede5ff0b5" />

