## Bandit Level 8 → Level 9

### Objective
Find the password for Bandit Level 9. The password is the only line in `data.txt` that occurs once.

### Commands Executed
- `ls`
- `sort data.txt | uniq -u`

### Explanation
- Used `ls` to list files in the directory.
- Sorted the contents of `data.txt` using `sort`.
- Used `uniq -u` to display the line that appears only once.
- The output revealed the password for the next level.

### Password

`4CKMh1JI91bUIZZPXDQGanaL4xvAg0JM`



<img width="552" height="92" alt="image" src="https://github.com/user-attachments/assets/c70e9c53-c359-46b8-a00e-46d162fb6a35" />

