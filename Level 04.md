# Level 3 → Level 4

## Objective
Find the password for Bandit Level 4. The password is stored in a hidden file inside a directory.

## Commands Executed
- `ls`
- `cd inhere`
- `ls -alph`
- `cat -- ...Hiding-From-You`

## Explanation
- Listed files in the current directory using `ls`.
- Entered the `inhere` directory.
- Used `ls -alph` to display all files, including hidden ones, with detailed information.
- Identified a hidden file named `...Hiding-From-You`.
- Used `cat --` to safely read the file since the filename starts with dots.
- The output revealed the password for Level 4.

## 🔐 Password Retrieved

2WmrDFRmJIg3IPxneAaMGhap0pFhF3NJ


<img width="774" height="229" alt="image" src="https://github.com/user-attachments/assets/677305c2-568b-4c83-a0c2-01b49cfca462" />

